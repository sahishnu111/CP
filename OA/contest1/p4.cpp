#include <bits/stdc++.h>
using namespace std;

long long maxLCTSubsequence(string s) {

    long long ans = 0;

    // write logic here

    return ans;
}

void solve() {
    string s;
    cin >> s;

    cout << maxLCTSubsequence(s) << "\n";
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;

    while(t--)
        solve();

    return 0;
}